# Extracts mp3 files.

# Currently this recipe works terribly because the header to match against is
# way too small. Therefore the command is executed very often, and it could
# take a whole day to complete.

# Depends on perl and mpg123 from http://www.mpg123.de/ 
0 char \xff
0 int32 FFfa0000 FFfe0000
extension mp3
command commands/mp3extract.pl $0
# Minimum output is 512KB, which should filter out junk 
min_output_file 512000
