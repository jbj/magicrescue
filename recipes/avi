# Extracts avi files.
# Depends on MPlayer's mencoder: http://www.mplayerhq.hu/
0 char R
0 string RIFF
# note the trailing space on the next line
8 string AVI 
# mencoder will keep reading after the avi ends, so we use commands/safecat to
# stop it when it's not outputting useful things anymore.
extension avi
command commands/safecat -d 30M -u 2M -t 200M $0|mencoder -nocache -o $0 -ovc copy -oac copy -
min_output_file 102400
